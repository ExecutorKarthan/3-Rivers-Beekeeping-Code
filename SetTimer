function SetTimer() {
  DeleteTimers();
  //3 hour timer
  ScriptApp.newTrigger("Main").timeBased().after(1000 * 60 * 60 * 3).create()
  //1 minute test timer
  //ScriptApp.newTrigger("Main").timeBased().after(1000 * 60).create()

}
